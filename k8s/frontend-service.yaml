apiVersion: v1
kind: Service
metadata:
  name: text2sql-frontend-service
spec:
  type: LoadBalancer # Exposes the frontend externally. For Minikube/local, NodePort can also be used.
  selector:
    app: text2sql-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
